# bauplan-sdk-types

This package contains the types required to define Pipelines in Bauplan. These types are just a facade, with no functionality attached. They are also available via the normal `bauplan` package, so these two statements are identical:

```python
from bauplan import Model
from bauplan_sdk_types import Model
```
