[project]
name = "bauplan-utils"
version = "0.1.0"
description = "Utilities to manage Bauplan"
readme = "README.md"
requires-python = "==3.13.*"
dependencies = [
    "bauplan~=0.1.10",
    "typer~=0.24.1",
]
