[project]
name = "bauplan-mcp-demo"
version = "0.1.0"
description = "AI-assisted data engineering with Claude Code and the Bauplan MCP server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bauplan>=0.1.11",
    "polars>=1.41.2",
]
